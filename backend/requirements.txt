# Pinned 2026-04-14 — reproducible builds
# alto-core is sibling-packaged in this monorepo. Install editable so
# in-tree edits land immediately; the path is resolved relative to the
# directory where `pip install -r` runs (cd into backend/ before installing,
# or use the matching WORKDIR in Dockerfile).
-e ../packages/alto-core
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.0,<3.0
httpx>=0.27,<1.0
lxml>=6.1,<7.0  # 6.1 closes PYSEC-2026-87
python-multipart>=0.0.12,<1.0
aiofiles>=24.1,<25.0
sse-starlette>=2.1,<3.0
anyio>=4.0,<5.0
